{
  "name": "applicable-ai",
  "private": true,
  "description": "npm script wrappers for the Python/Streamlit application",
  "scripts": {
    "dev": ".venv/bin/python -m streamlit run app.py",
    "setup": "python3 -m venv .venv && .venv/bin/python -m pip install -r requirements.txt",
    "test": ".venv/bin/python -m pytest"
  }
}
